name: CUDA smoke

# Spark has independent cpehle/tyr and ranvier-labs/tyr runner registrations.
# The dedicated upstream label avoids consuming old generic GPU jobs. Every
# qualification acquires the same host-wide lock; see docs/ci-and-qualification.md.
on:
  push:
    branches: [main]
    paths:
      - 'Tyr/GPU/**'
      - 'Tyr/Torch.lean'
      - 'Tests/*GPU*.lean'
      - 'Examples/GPU/**'
      - 'Examples/NanoChat/Generator/KVCache.lean'
      - 'Tyr/Inference/**'
      - 'Tyr/Model/Laguna/**'
      - 'Tests/RunLagunaModel.lean'
      - 'scripts/qualification/**'
      - 'scripts/ci/environment.sh'
      - 'scripts/test_native_attention.sh'
      - 'scripts/gpu/**'
      - 'cc/include/**'
      - 'cc/tools/**'
      - 'cc/src/tyr*'
      - 'cc/src/tk_*'
      - 'cc/src/generated/**'
      - 'cc/Makefile'
      - 'lakefile.lean'
      - 'lean-toolchain'
      - '.github/workflows/cuda-smoke.yml'
  pull_request:
    types: [labeled, opened, synchronize, reopened]
    paths:
      - 'Tyr/GPU/**'
      - 'Tyr/Torch.lean'
      - 'Tests/*GPU*.lean'
      - 'Examples/GPU/**'
      - 'Examples/NanoChat/Generator/KVCache.lean'
      - 'Tyr/Inference/**'
      - 'Tyr/Model/Laguna/**'
      - 'Tests/RunLagunaModel.lean'
      - 'scripts/qualification/**'
      - 'scripts/ci/environment.sh'
      - 'scripts/test_native_attention.sh'
      - 'scripts/gpu/**'
      - 'cc/include/**'
      - 'cc/tools/**'
      - 'cc/src/tyr*'
      - 'cc/src/tk_*'
      - 'cc/src/generated/**'
      - 'cc/Makefile'
      - 'lakefile.lean'
      - 'lean-toolchain'
      - '.github/workflows/cuda-smoke.yml'
  schedule:
    - cron: '23 5 * * 1'
  workflow_dispatch:
    inputs:
      real_models:
        description: Run pinned Qwen tokenizer parity and TTS/ASR qualification
        type: boolean
        default: true

permissions:
  contents: read

concurrency:
  group: cuda-smoke-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  readiness:
    if: github.event_name != 'pull_request' || contains(github.event.pull_request.labels.*.name, 'gpu-ci')
    runs-on: ubuntu-latest
    outputs:
      runner_labels: ${{ steps.configuration.outputs.runner_labels }}
    steps:
      - uses: actions/checkout@v4
      - name: Verify repository runner configuration
        id: configuration
        env:
          TYR_LIBTORCH_DIR: ${{ vars.TYR_LIBTORCH_DIR }}
          TYR_QUALIFICATION_RUNNER_LABELS: ${{ vars.TYR_QUALIFICATION_RUNNER_LABELS }}
        run: python3 scripts/qualification/readiness.py
      - name: Upload readiness evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qualification-readiness-${{ github.run_attempt }}
          path: output/qualification/readiness.json
          retention-days: 14

  gpu-smoke:
    needs: readiness
    runs-on: ${{ fromJSON(needs.readiness.outputs.runner_labels) }}
    timeout-minutes: 180
    env:
      GPU: ${{ vars.TYR_GPU || 'GB10' }}
      TYR_LIBTORCH_DIR: ${{ vars.TYR_LIBTORCH_DIR }}
      TYR_QUALIFICATION_ROOT: ${{ vars.TYR_QUALIFICATION_ROOT || '/home/pehle/tyr-qualification' }}
      TYR_QUALIFICATION_BOOTSTRAP_PYTHON: ${{ vars.TYR_QUALIFICATION_BOOTSTRAP_PYTHON || '/home/pehle/dev/tyr/.venv-gpu/bin/python' }}
      TYR_QUALIFY_MODELS: ${{ github.event_name == 'schedule' || inputs.real_models }}
      CUDA_HOME: ${{ vars.TYR_CUDA_HOME || '/usr/local/cuda' }}
    steps:
      - uses: actions/checkout@v4
      - name: Initialize required submodules
        run: git submodule update --init --depth 1 external/soxr thirdparty/ThunderKittens
      - name: Install pinned Lean toolchain
        run: |
          export PATH="$HOME/.elan/bin:$PATH"
          if ! command -v elan >/dev/null; then
            echo 'The qualification runner must have elan installed.' >&2
            exit 1
          fi
          toolchain="$(cat lean-toolchain)"
          if ! elan toolchain list | grep -Fxq "$toolchain"; then elan toolchain install "$toolchain"; fi
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Link runner-provided pinned CUDA runtime
        run: |
          python3 - <<'PYTHON'
          import os
          from pathlib import Path
          source = Path(os.environ['TYR_LIBTORCH_DIR'])
          if not (source / 'share/cmake/Torch/TorchConfigVersion.cmake').is_file():
              raise SystemExit('Configured CUDA LibTorch directory is missing')
          target = Path('external/libtorch')
          if target.is_symlink():
              target.unlink()
          elif target.exists():
              raise SystemExit('Refusing to replace an existing LibTorch directory; use a clean checkout')
          target.symlink_to(source.resolve(), target_is_directory=True)
          PYTHON
      - name: Build and run strict qualification with exclusive GPU access
        run: |
          mkdir -p "$TYR_QUALIFICATION_ROOT"
          flock --timeout 3600 "$TYR_QUALIFICATION_ROOT/gpu.lock" bash scripts/qualification/job.sh
      - name: Require executed qualification reports
        if: always()
        run: |
          reports=(gpu)
          if [[ "$TYR_QUALIFY_MODELS" == true ]]; then reports+=(models); fi
          python3 scripts/qualification/summarize.py --directory output/qualification "${reports[@]}"
      - name: Upload qualification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qualification-${{ runner.name }}-${{ github.run_attempt }}
          path: output/qualification/
          if-no-files-found: error
          retention-days: 30
